# Production environment example (AWS RDS MySQL)
# IMPORTANT: Do NOT commit real secrets. This file mirrors the provided values for operator convenience.
# If this repo is public or shared, remove/rotate any real credentials.

DB_HOST=db-lms-digitalt3.cejofalfd7i2.us-east-2.rds.amazonaws.com
DB_PORT=3306
DB_USERNAME=admin
DB_PASSWORD=REPLACE_ME
DEFAULT_DB=digitalt3_lms

AWS_REGION=us-east-2

# Enable SSL for AWS RDS. The backend will attempt to load `global-bundle.pem` automatically.
DB_SSL=true
DB_SSL_CA_PATH=

# Other required settings
JWT_SECRET=REPLACE_ME

# Place the AWS RDS CA bundle at:
#   lms_backend/global-bundle.pem
# or set DB_SSL_CA_PATH to its absolute/relative path.
